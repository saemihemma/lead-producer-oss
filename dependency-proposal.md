Do not read unless working on upstream dependency maintenance.

Last reviewed: 2026-03-23

## Changed upstream
- Initial baseline created for weekly review.

## Matters here
- This repo is a thin local adapter.
- Upstream packs are reviewed separately and selectively ported.
- External docs/design helpers stay optional.

## Recommend
- Review `superpowers`, `context7`, `brand-guidelines`, and `frontend-design` weekly against local routing and install contracts.
- Rewrite this file each run instead of appending history.

## Do nothing
- Do not auto-install, auto-pull, auto-merge, or rewrite local skills from upstream by default.
