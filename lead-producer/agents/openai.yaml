interface:
  display_name: "Lead Producer"
  short_description: "Route and stress-test multi-skill work"
  default_prompt: "Use $lead-producer to route this task, keep the team lean, and stress-test the result before accepting it."
